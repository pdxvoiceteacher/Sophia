This folder contains GitHub Actions workflows temporarily quarantined to stabilize CI.
To restore:
  Move files back into .github/workflows/
Then fix each workflow and re-enable required checks in branch protection.
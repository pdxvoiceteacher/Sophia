{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sophia/schema/recovery_recommendations_v1.schema.json",
  "title": "Recovery Recommendations v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "created_at", "phaselock", "caution", "recommendations"],
  "properties": {
    "schema": {"const": "recovery_recommendations_v1"},
    "created_at": {"type": "string"},
    "phaselock": {"type": "string"},
    "caution": {"type": "string"},
    "recommendations": {"type": "array", "items": {"$ref": "#/$defs/record"}}
  },
  "$defs": {
    "record": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "recoveryId",
        "recoveryStatus",
        "coherenceScore",
        "riskScore",
        "tamperRisk",
        "continuityContext",
        "antiCaptureContext",
        "governingRule",
        "explanation",
        "targetPublisherAction"
      ],
      "properties": {
        "recoveryId": {"type": "string"},
        "recoveryStatus": {
          "enum": [
            "maintain",
            "escrow-review",
            "watch",
            "hold",
            "reject",
            "require-human-emergency-review"
          ]
        },
        "coherenceScore": {"type": "number", "minimum": 0, "maximum": 1},
        "riskScore": {"type": "number", "minimum": 0, "maximum": 1},
        "tamperRisk": {"type": "number", "minimum": 0, "maximum": 1},
        "continuityContext": {"type": "object", "additionalProperties": true},
        "antiCaptureContext": {"type": "object", "additionalProperties": true},
        "governingRule": {"type": "string"},
        "explanation": {"type": "string"},
        "targetPublisherAction": {"enum": ["docket", "watch", "suppress"]}
      }
    }
  }
}
